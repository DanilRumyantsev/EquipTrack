import lang from "quasar/lang/ru"; // локализация (по желанию)
import iconSet from "quasar/icon-set/material-icons";

export default {
    lang: lang,
    iconSet: iconSet,
    plugins: {
        Notify: {}, // например, можно добавить плагины
    },
};
